# ============================================================================
#  Veeva Vault RIM - Submissions Archive bulk import
#  THIS IS THE ONLY FILE YOU NEED TO EDIT.
#
#  Both Import-VaultSubmissions.ps1 and Run-Import.bat read this file, so
#  there is never a second copy to keep in sync. Anything passed explicitly
#  on the command line overrides what is set here.
#
#  Format: KEY = VALUE, one per line. Lines starting with # are comments.
#
#  The dossiers are already on File Staging, under the Submissions Archive
#  root. Nothing is moved, downloaded or uploaded - each dossier is imported
#  from where it sits.
# ============================================================================


# ---- Required --------------------------------------------------------------

# Vault host name. No https://, no trailing slash.
VaultDNS = your-target-vault.veevavault.com

# File Staging folder holding the dossiers, under the Submissions Archive root.
# Everything matching Include below (recursively) is imported.
SourceStagingPath = /SubmissionsArchive/e157135

# Local folder for the results CSV and run log. The only thing written to disk.
OutputRoot = C:\Users\Sarah.Nassery


# ---- What this run should do -----------------------------------------------

# MANIFEST = list the dossiers, write manifest.csv, then stop. Nothing imported.
# DRYRUN   = resolve every submission id, but import nothing.
# IMPORT   = do it for real.
MODE = MANIFEST

# Optional sampling. Blank or 0 processes every submission found. A whole number
# 1-100 processes only that percentage of them, chosen at random - useful for
# spot-checking a large application without running the whole wave. The count is
# rounded up, so a small percentage still picks at least one submission.
#
# Each run draws a NEW random sample; it is not a fixed subset. Anything already
# imported is still skipped, and results from earlier runs are kept, so sampling
# repeatedly is safe - it just may re-pick submissions you already did.
SamplePercent =

# What to do when this run's CSV reports (import-results.csv, manifest.csv) are
# already in OutputRoot from an earlier run.
#   Prompt  = ask before starting (default). This is the safe choice: it shows what
#             is there - how many rows, how many succeeded, which application - and
#             lets you decide. A non-interactive host continues rather than asking.
#   Resume  = keep them. Anything already SUCCESS is skipped and its row preserved.
#   Restart = rotate them aside and begin fresh. The old file is RENAMED, never
#             deleted: import-results.csv -> import-results-<when written>.csv
#
# Restart when the reports in this folder are from a different application or a
# different wave; continue when you are re-running the same one.
ExistingResults = Prompt


# ---- Vault settings --------------------------------------------------------

# API version. One value moves the whole script between releases.
ApiVersion = v26.2

# Vault API session id. THE ONLY PLACE A SESSION ID IS SET.
# Leave blank to log in with username/password (the script manages the session,
# including re-authenticating if it expires mid-run). Paste a session id here to
# reuse one you already hold.
SessionId =


# ---- How submissions map to records ----------------------------------------
#
# By default NOTHING here is needed. Each submission folder name (0000, 0001, ...)
# and its application (the folder SourceStagingPath points at, e.g. e157135) are
# looked up in Vault by VQL - no manifest, no CSV. The three field names under
# Advanced are what that lookup uses; confirm they match your vault.
#
# The options below are only for when you want to override that automatic lookup.

# Optional hand-filled mapping sheet (FileName -> SubmissionId). Local path.
# Overrides the automatic lookup for any row it lists.
Manifest =

# Optional: an export_results.csv, if you have one. Overridden by Manifest.
ExportResultsCsv =
IdColumn =
PathColumn =

# Set true only if each submission is named for its record id, e.g. 00S...001.zip
NameIsSubmissionId = false


# ---- Advanced - sensible defaults, change only if you have a reason ---------

# How a folder name (0000) and application (e157135) map onto Vault fields for
# the automatic lookup. Confirm these three against your vault's object model.

# submission__v field the folder name is found in. In this vault the Submission
# Name is "0000 - Submission Meeting Minutes", so the folder "0000" is a PREFIX of
# name__v (see SubmissionMatch below).
LookupField = name__v

# How the folder name matches LookupField:
#   prefix = name__v starts with the folder name, e.g. "0000 - ..."   (this vault)
#   exact  = LookupField holds just the number, e.g. "0000"
SubmissionMatch = prefix

# Resolving the application (e157135) is done in two steps to avoid VQL
# relationship traversal:
#   1. find the application record:  SELECT id FROM <ApplicationObject>
#                                    WHERE <ApplicationKeyField> = 'e157135'
#   2. filter submissions by it:     ... AND <ApplicationRefField> = '<that id>'

# object queried to turn e157135 into a record id
ApplicationObject = application__v

# field on the application object holding the folder name (e157135)
ApplicationKeyField = name__v

# field on submission__v that stores the application id (direct equality, no traversal)
ApplicationRefField = application__v

# Used when a manifest row leaves DossierFormatId blank.
DefaultDossierFormatId =

# Which staged files count as dossiers.
Include = *.zip, *.tar.gz, *.tgz

JobTimeoutMinutes = 120
JobPollSeconds    = 20
MaxRetries        = 4
